"""xyz.positions — position management helpers for the engine.

In v1 this package contains only a thin manual-entry helper
(manual_entry.insert_positions_bulk) used for CSV imports and ad-hoc
seeding.  Future tasks add position reconciliation and broker-sync logic.
"""
